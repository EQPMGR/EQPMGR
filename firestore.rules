
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This is a temporary, insecure rule for debugging purposes only.
    // It allows all reads and writes to the entire database.
    match /{document=**} {
      allow read, write;
    }
  }
}
