rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: INSECURE - FOR DIAGNOSTIC PURPOSES ONLY
    // These rules allow any user to read and write to the entire database.
    // We will replace these with secure rules once we confirm the application
    // code is working correctly.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
