
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: TEMPORARILY INSECURE RULE FOR DEBUGGING
    // This allows anyone to read and write to the database.
    // This MUST be changed back to a secure rule.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
