
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads and writes by any authenticated user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
